# Nebulai Homebrew tap

```bash
brew install kaiservermillion/nebulai/nebulai
nebulai install
```

`nebulai install` deploys a Nebulai installation into your own Azure subscription using
your own `az` session. See the [installation runbook](https://github.com/KaiserVermillion/nebulai-releases).
